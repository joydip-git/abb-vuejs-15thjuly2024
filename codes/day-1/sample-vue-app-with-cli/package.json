{
  "name": "sample-vue-app-with-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider && vite",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && vite build",
    "preview": "set NODE_OPTIONS=--openssl-legacy-provider && vite preview",
    "lint": "set NODE_OPTIONS=--openssl-legacy-provider && eslint . --ext .vue,.js,.jsx,.cjs,.mjs --fix --ignore-path .gitignore"
  },
  "dependencies": {
    "vue": "^3.4.29"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.5",
    "@vitejs/plugin-vue-jsx": "^4.0.0",
    "eslint": "^8.57.0",
    "eslint-plugin-vue": "^9.23.0",
    "vite": "^5.3.1"
  }
}